define([], function () {
    /**
     * <%= description %> 
     * @alias <%= constructor %> 
     * @constructor
     * @param {Object} options - options object
     */
    var <%= constructor %> = function(options){

    };

    return <%= constructor %>;
});